Homebrew tap for [wizard](https://github.com/teddytennant/wizard).

    brew trust teddytennant/tap && brew install teddytennant/tap/wizard

The formula installs the signed release binary for macOS and Linux (arm64 and x86_64); `Formula/wizard.rb` is rewritten by the wizard release workflow on every tag.
